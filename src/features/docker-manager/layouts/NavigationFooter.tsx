import React from "react";import { FC } from "react";
interface INavigationFooterProps {};

export const NavigationFooter: FC<INavigationFooterProps> = (props) => {
    return (
        <div>
            NavFooter
        </div>
    );
}
