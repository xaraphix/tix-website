import React from "react";
import { FC } from "react";
interface IContainersProps {};

export const Containers: FC<IContainersProps> = (props) => {
    return (
        <div>
            Containers
        </div>
    );
}
