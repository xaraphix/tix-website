import React from "react";
import { FC } from "react";
interface IImagesProps {};

export const Images: FC<IImagesProps> = (props) => {
    return (
        <div>
            Images
        </div>
    );
}
